import os
os.system("clear")
import androidhelper
droid = androidhelper.Android()
droid.makeToast('منوريين سكربت الشبح..!')

def v7x (name) :
    name = input ("type your name: ")
    if name == 'haedr' :
        print('welcome', name)
    else:
        print (" Error" *100)
v7x ("ha")
